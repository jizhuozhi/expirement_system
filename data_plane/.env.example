# Data Plane Configuration

# Directory paths
LAYERS_DIR=../configs/layers
EXPERIMENTS_DIR=../configs/experiments
FIELD_TYPES_FILE=../configs/field_types.json

# Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080       # HTTP port for health check and metrics
GRPC_PORT=50051        # gRPC port for experiment assignment (main API)
METRICS_PORT=9090

# Config source mode
# - If not set: use file watching mode
# - If set: connect to control plane via gRPC
CONTROL_PLANE_ADDR=

# Logging
RUST_LOG=info,experiment_data_plane=debug
